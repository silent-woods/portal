@model BankAccountModel

@{
    Layout = "_AdminLayout";
    ViewBag.PageTitle = T("Satyanam.Plugin.Misc.AccountManagement.Admin.BankAccount.Add").Text;
    NopHtml.SetActiveMenuItemSystemName("Satyanam.Plugin.Misc.AccountManagement.Admin.BankAccounts");
}

<form asp-controller="AccountManagement" asp-action="BankAccountCreate" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Satyanam.Plugin.Misc.AccountManagement.Admin.BankAccount.Add")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="BankAccounts" asp-controller="AccountManagement">@T("Satyanam.Plugin.Misc.AccountManagement.Admin.BankAccount.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(AccountManagementPermissionProvider.ManageBankAccounts, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("_CreateOrUpdateBankAccount.cshtml", Model)
</form>
