@using Satyanam.Nop.Plugin.SatyanamCRM.Models.LinkedInFollowups
@model LinkedInFollowupsModel
@{
    Layout = "_AdminLayout";
}
@{
    //page title
    ViewBag.PageTitle = T("Plugin.SatyanamCRM.LinkedInFollowups.Create").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("LinkedInFollowups");
}

<form asp-controller="LinkedInFollowups" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Plugin.SatyanamCRM.LinkedInFollowups.Create")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Plugin.SatyanamCRM.LinkedInFollowups.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(SatyanamPermissionProvider.ManageLinkedInFollowups, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("~/Plugins/Misc.SatyanamCRM/Views/LinkedInFollowups/_CreateOrUpdate.cshtml", Model)
</form>
