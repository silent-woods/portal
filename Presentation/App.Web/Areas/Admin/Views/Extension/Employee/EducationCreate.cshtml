@model EducationModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.Employee.Education.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Employee");
}
<form asp-controller="Employee" asp-action="EducationCreate" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Employee.Education.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-controller="Employee" asp-action="Edit" asp-route-id="@Model.EmployeeID">@T("Admin.Employee.Education.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageEducation, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/Employee/_EmployeeEducationMapping.cshtml", Model)
</form>