@model UpdateTemplateModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.UpdateTemplate.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("UpdateTemplate");
}

<form asp-controller="UpdateTemplate" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.UpdateTemplate.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.UpdateTemplate.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(SatyanamPermissionProvider.ManageUpdateTemplate, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/UpdateTemplate/_CreateOrUpdate.cshtml", Model)
</form>
