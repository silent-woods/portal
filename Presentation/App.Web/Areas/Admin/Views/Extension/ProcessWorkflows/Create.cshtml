@using App.Web.Areas.Admin.Models.Extension.ProcessWorkflows;

@model ProcessWorkflowModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.Configuration.ProcessWorkflow.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("ProcessWorkflow");
}
<form asp-controller="ProcessWorkflow" asp-action="Create" method="post">

    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Configuration.Project.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.Configuration.ProcessWorkflow.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageProcessWorkflow, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/ProcessWorkflows/_CreateOrUpdate.cshtml", Model)
</form>
