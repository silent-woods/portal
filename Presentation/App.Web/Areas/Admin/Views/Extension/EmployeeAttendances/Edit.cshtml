@model EmployeeAttendanceModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.Configuration.EmployeeAttendance.EditEmployeeAttendanceDetails").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("EmployeeAttendance");
}
<form asp-controller="EmployeeAttendance" asp-action="Edit" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Configuration.EmployeeAttendance.EditEmployeeAttendanceDetails")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.Configuration.EmployeeAttendance.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageEmployeeAttendance, PermissionAction.Edit))
            {
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            }
            @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageEmployeeAttendance, PermissionAction.Delete))
            {
                <span id="employeeAttendance-delete" class="btn btn-danger">
                    <i class="far fa-trash-alt"></i>
                    @T("Admin.Common.Delete")
                </span>
            }
        </div>
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/EmployeeAttendances/_CreateOrUpdate.Info.cshtml", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="employeeAttendance-delete" />