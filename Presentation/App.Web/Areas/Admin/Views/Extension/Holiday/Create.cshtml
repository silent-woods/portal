@model HolidayModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.HRM.Holidays.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Holiday");
}

<form asp-controller="Holiday" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.HRM.Holidays.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.HRM.Holidays.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageHoliday, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/Holiday/_CreateOrUpdate.cshtml", Model)
</form>
