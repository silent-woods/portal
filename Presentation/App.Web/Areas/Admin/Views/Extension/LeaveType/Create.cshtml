@model LeaveTypeModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.Configuration.LeaveType.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("LeaveType");
}
<form asp-controller="LeaveType" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Configuration.LeaveType.AddNew")
            <small>
                <i class="fas fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.Configuration.LeaveType.BackToList")</a>
            </small>
        </h1>
        @if (await permissionService.AuthorizeAsync(StandardPermissionProvider.ManageLeaveType, PermissionAction.Add))
        {
            <div class="float-right">
                <button type="submit" name="save" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.Save")
                </button>
                <button type="submit" name="save-continue" class="btn btn-primary">
                    <i class="far fa-save"></i>
                    @T("Admin.Common.SaveContinue")
                </button>
            </div>
        }
    </div>
    @await Html.PartialAsync("/Areas/Admin/Views/Extension/LeaveType/_CreateOrUpdate.cshtml", Model)
</form>
