@using App.Web.Areas.Admin.Models.Extension.SkillSet
@model SkillSetModel
@{
    const string hideInfoBlockAttributeName = "SkillSetDetails.HideInfoBlock";
    var customer = await workContext.GetCurrentCustomerAsync();
    var hideInfoBlock = await genericAttributeService.GetAttributeAsync<bool>(customer, hideInfoBlockAttributeName);
}
<input asp-for="Id" type="hidden" />
<section class="content">
    <div class="container-fluid">
        <div class="form-horizontal">
            <nop-cards id="SkillSet-item-cards">
                <nop-card asp-name="SkillSet-item-info"
                          asp-icon="fas fa-info" 
                          asp-title="@T("Admin.SkillSet.Info")"
                          asp-hide-block-attribute-name="@hideInfoBlockAttributeName"
                          asp-hide="@hideInfoBlock">
                    @await Html.PartialAsync("/Areas/Admin/Views/Extension/SkillSet/_CreateOrUpdate.Info.cshtml", Model)
                </nop-card>
            </nop-cards>
        </div>
    </div>
</section>
